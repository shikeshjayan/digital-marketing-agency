# API base URL (optional — defaults to /api/v1)
# In development with Vite proxy, leave unset or commented out
# In production, set to your Vercel deployment URL
# VITE_API_URL=https://your-app.vercel.app/api/v1
